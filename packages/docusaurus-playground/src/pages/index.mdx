import {
  Hero,
  HeroTitle,
  HeroDescription,
  HeroActions,
  HeroAction,
  CallToActionSection,
  CallToActionButton,
  FeatureList,
  Showcase,
  HeroModel,
  HeroInfo,
  Box,
} from '../components/mdx'

{' '}

<HeroModel
  modelId={'bust01'}
  mode={'abstract'}
  rotateSpeed={0.04}
  startY={'bottom'}
  asciiConfig={{
    characters: ' + ^ ^%%%0 -a @@@ ###',
  }}
/>
