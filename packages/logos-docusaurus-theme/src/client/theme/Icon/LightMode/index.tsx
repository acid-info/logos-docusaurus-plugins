import type { Props } from '@theme/Icon/LightMode'
import React from 'react'
import Icon from '../../../static/icons/day.svg'

export default function IconLightMode(props: Props): JSX.Element {
  return <Icon {...props} />
}
