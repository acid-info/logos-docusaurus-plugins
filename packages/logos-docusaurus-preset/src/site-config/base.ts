import { SiteConfig } from '../index'

export const baseSiteConfig: SiteConfig = {
  title: 'Title',
  tagline: 'Tagline',
  url: 'http://docs',
  favicon: 'theme/img/favicon.ico',
  onBrokenLinks: 'warn',
  onBrokenMarkdownLinks: 'warn',
}

export default baseSiteConfig
